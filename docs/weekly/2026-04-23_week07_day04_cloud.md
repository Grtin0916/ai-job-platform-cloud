# 2026-04-23 Week07 Day04 Cloud

## 1. Goal

把 Cloud 仓从“Week07 最小 CI 骨架已落盘”推进到“本地最小验证链已再次复验并可直接引用”的状态。

今天的重点不是继续扩写完整 CD，而是确认：

- `.github/workflows/ci.yml` 已存在
- `scripts/ci_validate.sh` 已可执行并通过
- `docker compose -f docker-compose.observability.yml config` 可通过
- `kubectl apply --dry-run=client -f k8s/base` 可通过
- README / runbook / weekly / 日志证据保持同口径

## 2. What was verified

本次本地复验覆盖以下动作：

- 核对仓库根目录、远端与关键文件存在性
- 执行 `./scripts/ci_validate.sh`
- 执行 `docker compose -f docker-compose.observability.yml config`
- 执行 `kubectl apply --dry-run=client -f k8s/base`
- 复核 README 顶部三段与当前仓库状态是否一致

## 3. Verified findings

本次确认：

- `README.md`、`.github/workflows/ci.yml`、`scripts/ci_validate.sh`、`docs/runbooks/ci-cd-minimum.md`、`docker-compose.observability.yml` 均存在
- `./scripts/ci_validate.sh` 通过
- `docker compose -f docker-compose.observability.yml config` 通过
- `kubectl apply --dry-run=client -f k8s/base` 通过
- 当前 Compose 配置已可展开 Grafana 与 Prometheus 两个服务
- `k8s/base` 当前至少包含：
  - `namespace/dev-platform`
  - `deployment.apps/gateway`
  - `service/gateway`

## 4. Meaning

这说明 Cloud 仓当前已经不再只是“OTel 最小链路实验仓”，而是进入“最小 CI/CD 交付入口已可复验”的阶段。

当前最值钱的结果不是继续假装已经有完整发布平台，而是把以下边界写清楚：

- 本地 repo validation 已成立
- Compose config 校验已成立
- K8s client dry-run 已成立
- Workflow / runbook / README / 日志之间没有明显脱节

## 5. Not done yet

以下内容仍未完成，不应提前写成已完成：

- 真实 image build / push
- Terraform init / validate / plan
- 真实集群部署、rollout 与 rollback
- 更系统的 alerts / SLO / release policy
- 更完整的 CI/CD 发布闭环

## 6. Evidence

建议引用的证据文件包括：

- `artifacts/logs/week07_ci_validate_local_rerun.log`
- `artifacts/logs/week07_compose_config_rerun.log`
- `artifacts/logs/week07_k8s_dry_run_rerun.log`

## 7. Next hard move

下一步不应马上扩成完整 CD，而应先做两件事：

1. 同步 README / weekly / rerun 日志，使 Week07 证据收口到可直接引用的版本
2. 以 W8 为入口，开始明确 Terraform 布局与更真实的交付对象边界
