# 2026-04-03 week01 cloud

## 1. 本周目标

本周目标是把 `ai-job-platform-cloud` 从 0 起到一个可运行、可说明、可扩展的最小 Cloud 工程骨架，并完成第一次本地 `kind + kubectl apply` 留痕，而不是提前进入完整 observability、CI/CD 或真实云资源管理。

## 2. 本次完成内容

本次已完成：

- 新建 Cloud 独立仓库目录骨架
- 冻结本地集群方案为 `kind`
- 本地安装并验证：
  - Docker
  - Docker Compose
  - kind
  - kubectl
  - Terraform
- 完成 `scripts/bootstrap_dev_env.sh`
- 完成 Terraform 目录说明文档
- 完成 Kubernetes base 样板：
  - `namespace.yaml`
  - `gateway-deployment.yaml`
- 完成第一次本地 K8s 样板验证留痕：
  - `kubectl apply -f k8s/base`
  - `kubectl get deploy,svc -n dev-platform`
  - `kubectl describe deploy gateway -n dev-platform`
  - `kubectl describe svc gateway -n dev-platform`

## 3. 当前工程判断

当前仓库已经具备“最小本地 Cloud 开发骨架 + 第一次本地 K8s 样板留痕”的条件，但仍处于第一阶段。

当前优点：

- 工具链明确
- 本地集群方案已冻结
- Terraform 与 K8s 目录边界已固定
- 已开始形成可引用日志证据
- 后续扩展 observability、部署、回滚时不会从零返工

当前局限：

- 目前只完成了最小样板验证，尚未形成完整服务链路
- 尚未接入 Prometheus / Grafana / OpenTelemetry
- 尚未完成 Terraform `envs/dev` 的最小入口文件验证
- 尚未形成 CI/CD 与 runbook
- 尚未将 Java 服务正式部署到 dev K8s

## 4. 证据链

当前可引用证据包括：

- `README.md`
- `scripts/bootstrap_dev_env.sh`
- `infra/terraform/README.md`
- `k8s/base/namespace.yaml`
- `k8s/base/gateway-deployment.yaml`
- `artifacts/logs/week04_bootstrap_dev_env.log`
- `artifacts/logs/week04_k8s_apply_namespace.log`
- `artifacts/logs/week04_k8s_apply_gateway.log`
- `artifacts/logs/week04_k8s_get_resources.log`
- `artifacts/logs/week04_k8s_describe_gateway.log`
- `artifacts/logs/week04_k8s_describe_gateway_svc.log`

## 5. 下一步

下一步按周计划推进：

1. 补 `infra/terraform/envs/dev` 最小入口
2. 对 `terraform fmt` / `terraform validate` 做第一次本地校验
3. 把 Java 服务部署到 dev K8s
4. 开始准备 observability 最小链路
5. 增加 rollout / rollback / runbook