# 2026-04-03 week01 cloud

## 1. 本周目标

本周目标是把 `ai-job-platform-cloud` 从 0 起到一个可运行、可说明、可扩展的最小 Cloud 工程骨架，而不是提前进入完整 observability、CI/CD 或真实云资源管理。

## 2. 本次完成内容

本次已完成：

- 新建 Cloud 独立仓库目录骨架
- 冻结本地集群方案为 `kind`
- 本地安装并验证：
  - Docker
  - Docker Compose
  - kind
  - kubectl
  - Terraform
- 完成 `scripts/bootstrap_dev_env.sh`
- 完成 Terraform 目录说明文档
- 完成 Kubernetes base 样板：
  - `namespace.yaml`
  - `gateway-deployment.yaml`

## 3. 当前工程判断

当前仓库已经具备“最小本地 Cloud 开发骨架”的条件，但仍处于第一阶段。

当前优点：

- 工具链明确
- 本地集群方案已冻结
- Terraform 与 K8s 目录边界已固定
- 后续扩展 observability、部署、回滚时不会从零返工

当前局限：

- 尚未创建本地 kind 集群
- 尚未验证真实 deployment rollout
- 尚未接入 Prometheus / Grafana / OpenTelemetry
- 尚未接 Terraform envs/dev 的真实入口文件
- 尚未形成 CI/CD 与 runbook

## 4. 证据链

当前可引用证据包括：

- `README.md`
- `scripts/bootstrap_dev_env.sh`
- `infra/terraform/README.md`
- `k8s/base/namespace.yaml`
- `k8s/base/gateway-deployment.yaml`
- 工具版本检查输出

## 5. 下一步

下一步按周计划推进：

1. 创建本地 kind 集群
2. 验证 `kubectl apply -f k8s/base`
3. 补 `infra/terraform/envs/dev` 最小入口
4. 开始准备 observability 最小链路
5. 为后续部署 Java 服务预留 runbook
